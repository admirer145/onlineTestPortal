import { CustomFormValidations } from './custom-form-validations';

describe('CustomFormValidations', () => {
  it('should create an instance', () => {
    expect(new CustomFormValidations()).toBeTruthy();
  });
});
